import LightRawTheme = require('material-ui/lib/styles/raw-themes/light-raw-theme');
import ThemeManager = require('material-ui/lib/styles/theme-manager');

export const theme = ThemeManager.getMuiTheme(LightRawTheme);